# Modified requirements for Python 3.13 compatibility
# Some packages like TTS don't support 3.13 yet, so we'll use alternatives/workarounds

# Core dependencies that work with Python 3.13
gradio>=4.0.0                    # Simple UI - works with 3.13
torch>=2.0.0                     # Core DL - works with 3.13
transformers>=4.30.0             # TinyLlama chat model - works with 3.13
sentencepiece>=0.1.99            # Tokenizer dep - works with 3.13
soundfile>=0.12.1                # Write wav - works with 3.13
sounddevice>=0.4.6               # Mic capture - works with 3.13
numpy>=1.24.0                    # Updated for Python 3.13
opencv-python>=4.8.0             # For avatar generation - works with 3.13

# Speech-to-text: Use openai-whisper instead of faster-whisper for 3.13 compatibility
openai-whisper>=20231117         # Official OpenAI Whisper

# TTS workaround: TTS doesn't support 3.13, but we have fallback code
# Commenting out for now, the code has silent audio fallback
# TTS>=0.20.0                    # XTTS-v2 voice cloning (NOT compatible with Python 3.13)

# Try installing TTS anyway - it might work or fail gracefully
# The code has fallback to silent audio generation
